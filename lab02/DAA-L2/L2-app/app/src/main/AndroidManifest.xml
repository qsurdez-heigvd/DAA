<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DAA"
        tools:targetApi="34" >
        <!-- This activity is used to display the fragment stack -->
        <activity
            android:name=".FragmentStackActivity"
            android:exported="false" />
        <!-- This acitivity is used from the MainActivity to edit the name -->   
        <activity
                android:name=".EditNameActivity"
                android:exported="false"/>
        <!-- This activity is used to display the fragment counter and color chooser -->
        <activity
            android:name=".FragmentActivity"
            android:exported="false" />
        <!-- This activity is the main activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>